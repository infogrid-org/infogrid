To debug:
 * invoke from the command-line: mvn generate-sources
 * with env var: MAVEN_DEBUG_OPTS='-Xdebug -Xrunjdwp:transport=dt_socket,address=8888,server=y,suspend=y'
 * and then connect with NetBeans debugger at localhost 8888